# Container-side runtime deps for the ATDM agent (FastAPI).
# Keep in lockstep with the project root pyproject.toml dev deps.
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
httpx>=0.27
python-ulid>=3
pyarrow>=18,<21
minio>=7.2
pyyaml>=6.0
# Phase 8: server-rendered audit UI + Prometheus metrics.
jinja2>=3.1
prometheus-client>=0.20
